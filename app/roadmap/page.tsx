export const metadata = {
    title: "Product Roadmap — AI Inbox Assistant",
    description: "Follow our progress as we build AI Inbox Assistant. See what's shipped, what's in progress, and what's coming next.",
};

import RoadmapClient from "./RoadmapClient";

export default function RoadmapPage() {
    return <RoadmapClient />;
}
